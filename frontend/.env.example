# Internal URL of the FastAPI service; the browser only ever talks to same-origin /api/*
API_INTERNAL_URL=http://localhost:8000
# Port the standalone server listens on (Railway injects this)
PORT=3000
